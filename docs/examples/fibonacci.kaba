// A simple fibonacci implementation

fn main() do
    debug fibonacci(6);
end

fn fibonacci(n: int): int do
    if n <= 0 do
        return 0;
    else if n == 1 || n == 2 do
        return 1;
    end

    return fibonacci(n-1) + fibonacci(n-2);
end
